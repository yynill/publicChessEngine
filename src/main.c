#include "a_header.h"

// compile with         "make"
// run with             "./build/main"
int main(int argc, char *argv[])
{
    if (argc == 1)
    {
        mainAuxRunGameGUI();
    }
    return 0;
}
